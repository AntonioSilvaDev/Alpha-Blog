<h1>Edit an Article</h1>

<% if @article.errors.any? %>
  <h4 class="text-red">The following errors prevented the article from being created:</h4>
  <ul>
    <% @article.errors.full_messages.each do |msg| %>
      <li><%= msg %></li>
    <% end %>
  </ul>
<% end %>

<%= render 'form' %>